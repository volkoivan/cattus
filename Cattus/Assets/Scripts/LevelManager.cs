using UnityEngine;

public class LevelManager: MonoBehaviour {

    // Use this for initialization
    private void Start() {
        audio.Play();
    }

    // Update is called once per frame
    private void Update() {
    }
}